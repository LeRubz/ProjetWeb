{% extends 'layout.html.twig' %}

{% block title %}Entreprises{% endblock %}

{% block content %}

<section class="entreprise-preview-container">
    <div class="entreprise-preview">

        {% for company in companies %}
            <div class="entreprise-card-v2">
                <img src="{{ company.banner_path }}" alt="{{ company.name }} bannière" class="banner-img">
                <div class="logo-entreprise-mini">
                    <img src="{{ company.logo_path }}" alt="Logo {{ company.name }}">
                </div>
                <div class="contenu-entreprise-card">
                    <h3>{{ company.name }} recrutement</h3>
                    <div class="tags">
                        <span class="tag">{{ company.industry }}</span>
                        <span class="tag">{{ company.city }}</span>
                        <span class="tag">~{{ company.employees }} salariés</span>
                    </div>
                    <div class="bas-carte">
                        <button class="btn-decouvrir" onclick="openModal('modal-{{ company.id }}')">Voir plus</button>
                        <span class="nb-jobs"><strong>{{ company.nb_offer }}</strong> jobs</span>
                    </div>
                </div>
            </div>

            <!-- 🪟 Modale d'informations -->
            <div id="modal-{{ company.id }}" class="modal hidden">
                <div class="modal-content">
                    <span class="close" onclick="closeModal('modal-{{ company.id }}')">&times;</span>
                    <h2>{{ company.name }}</h2>
                    <p><strong>Description :</strong> {{ company.descr }}</p>
                    <p><strong>Ville :</strong> {{ company.city }}</p>
                    <p><strong>Secteur :</strong> {{ company.industry }}</p>
                    <p><strong>Nombre d'employés :</strong> {{ company.employees }}</p>
                    <p><strong>Âge moyen :</strong> {{ company.middle_ages }} ans</p>
                    <p><strong>Offres disponibles :</strong> {{ company.nb_offer }}</p>
                </div>
            </div>
        {% endfor %}




    </div>
</section>

<section class="faq-entreprise">
    <div class="faq-header">
        <h2>Les questions<br><span>les plus fréquentes sur les entreprises</span></h2>
        <p>
            Vous recherchez une entreprise qui vous permettra de progresser et de donner le meilleur de vous-même ?
            Vous avez de nouvelles attentes professionnelles ?<br>
            N’hésitez plus et donnez un nouveau souffle à votre carrière. Internhub vous aide à trouver le travail qui vous correspond.<br>
            Nous vous proposons ci-dessous des annonces d’emploi classées par entreprises et vous montrons quelles entreprises recrutent le plus par ville ou par métier.<br>
            Internhub est votre partenaire tout au long de votre carrière professionnelle.
        </p>
    </div>
</section>

{% endblock %}
